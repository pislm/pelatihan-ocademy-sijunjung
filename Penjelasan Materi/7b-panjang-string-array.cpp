#include <iostream>
using namespace std;

int main() {
    string a = "halo dunia";
    for (int i = 0; i < a.length(); i++) {
        cout << a[i] << endl;
    }

    // int arr[5] = {4, 5, 6, 7, 4};
    // cout << "Sizeof arr " << sizeof(arr) << endl;
    // cout << "Sizeof elemen pertama " << sizeof(arr[0]) << endl;
    // cout << "Panjang array " << sizeof(arr) / sizeof(arr[0]) << endl;
}